# =====================================================
# Barrierefreiheit-Plattform - Umgebungsvariablen
# =====================================================
# Kopieren Sie diese Datei nach .env und passen Sie die Werte an:
#   cp .env.example .env
#
# WICHTIG: Fügen Sie .env zur .gitignore hinzu!
# =====================================================

# ----- OpenAI API (ERFORDERLICH für KI-Funktionen) -----
# Wird benötigt für: Alt-Text-Generator, Leichte Sprache, Untertitel-Transkription
# API-Key erhalten unter: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# ----- Anthropic API (OPTIONAL) -----
# Alternative für KI-Funktionen mit Claude
# API-Key erhalten unter: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ----- Domain-Konfiguration (für Traefik/HTTPS) -----
# Ihre Domain für die Plattform
DOMAIN=barrierefreiheit.example.de

# E-Mail für Let's Encrypt Zertifikate
ACME_EMAIL=admin@example.de

# ----- Traefik Dashboard (OPTIONAL) -----
# Basic Auth für Traefik Dashboard
# Generieren mit: htpasswd -nb admin sicheres-passwort
TRAEFIK_AUTH=admin:$apr1$...

# ----- Anwendungs-Einstellungen -----
# Node.js Umgebung (production/development)
NODE_ENV=production

# Port (Standard: 3000)
PORT=3000

# ----- Datenschutz (DSGVO) -----
# Alle Daten werden nur temporär verarbeitet und nach der Sitzung gelöscht.
# Es werden keine Benutzerdaten gespeichert.
# KI-Anfragen werden an OpenAI/Anthropic gesendet - siehe deren Datenschutzrichtlinien.
